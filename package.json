{
  "name": "edge-slash-multi-search",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "check": "node --check engines.js && node --check background.js && node --check split.js",
    "test": "node --test"
  }
}
